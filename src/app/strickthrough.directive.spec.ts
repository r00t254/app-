import { StrickthroughDirective } from './strickthrough.directive';

describe('StrickthroughDirective', () => {
  it('should create an instance', () => {
    const directive = new StrickthroughDirective();
    expect(directive).toBeTruthy();
  });
});
